<?xml version="1.0" encoding="utf-8"?>
<!-- 1. 根布局是 ScrollView，确保小屏能滚动 -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:padding="16dp">

    <!-- 2. 内部使用 LinearLayout 纵向排列（简单直观） -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- 标题：TextView -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📚 学习资料登记"
            android:textSize="28sp"
            android:textStyle="bold"
            android:layout_marginTop="32dp"
            android:textColor="#FF5722" />

        <!-- 输入框：EditText -->
        <EditText
            android:id="@+id/etMaterialName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:hint="请输入资料名称（如：Android 基础）"
            android:inputType="text" />

        <!-- 图片：ImageView（用默认的启动图标占位） -->
        <ImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginTop="24dp"
            android:src="@drawable/jason_hu_unsplash"
            android:scaleType="centerCrop"
            android:contentDescription="资料封面" />

        <!-- 进度条：ProgressBar（水平样式） -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="登记进度"
            android:textSize="16sp" />

        <ProgressBar
            android:id="@+id/progressBar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="20dp"
            android:layout_marginTop="8dp"
            android:max="100"
            android:progress="0" />

        <!-- ⚠️ 注意：这里的 ID 是 btnRegister（后面故意写错） -->
        <Button
            android:id="@+id/btnRegister"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:text="提交登记"
            android:paddingLeft="32dp"
            android:paddingRight="32dp" />

    </LinearLayout>
</ScrollView>